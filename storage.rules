rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // CMS and media uploads use persistent Storage URLs.
    // Keep access simple: any authenticated session can read and write.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
