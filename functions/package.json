{
  "name": "focus-club-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .js,.ts src"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.5.0",
    "googleapis": "^173.0.0",
    "resend": "^6.12.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
